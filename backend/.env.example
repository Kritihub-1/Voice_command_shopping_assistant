# Backend Environment Configuration
# Copy to .env.production and update with your actual values

# Flask Configuration
FLASK_ENV=development
FLASK_APP=run.py
FLASK_DEBUG=1
PORT=5000

# API Configuration
API_URL=http://localhost:5000
API_WORKERS=4

# Frontend Configuration (if backend needs to know where frontend is)
FRONTEND_URL=http://localhost:3000

# Database (if you add database later)
# DATABASE_URL=your_database_url

# API Keys (if you add external services)
# OPENAI_API_KEY=your_key
# OTHER_SERVICE_KEY=your_key
